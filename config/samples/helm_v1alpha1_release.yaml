apiVersion: helm.soer3n.info/v1alpha1
kind: Release
metadata:
  name: release-sample
  namespace: helm
spec:
  name: nextcloud
  namespace:
    install: true
    name: bar
  repo: nextcloud
  chart: nextcloud
  version: 2.6.5
  releaseSpec:
    valueRefs:
    - values-sample
